import { StyleSheet } from "react-native";

const styles_BackButton = StyleSheet.create({
    arrowBox: {
        width: 50,
        height: 50,
        backgroundColor: "#fef5e8",
        borderRadius: 10,
        marginTop: 32,
        marginLeft: 20,
        justifyContent: "center",
        alignItems: "center"
    }
})

export default styles_BackButton
