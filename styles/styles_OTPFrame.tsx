import { StyleSheet } from "react-native";

const Styles_OTPFrame = StyleSheet.create({
    main: {
        flexDirection: "row",
        justifyContent: "space-between"
    },
    each: {
        width: 75.5,
        height: 68,
        backgroundColor: "#fef5e8",
        borderRadius: 12
    }
})

export default Styles_OTPFrame
