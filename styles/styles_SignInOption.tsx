import { StyleSheet } from "react-native";

const stylesSignInOption = StyleSheet.create({
    main: {
        flexDirection: "row",
        width: 150,
        height: 45,
        borderColor: "#e2e0de",
        borderWidth: 1,
        borderRadius: 7,
        alignItems: "center",
        justifyContent: "center"
    },

    icon: {
        marginRight: 10
    }
})

export default stylesSignInOption
